name = "promptops-api"
main = "src/index.ts"
compatibility_date = "2026-03-02"

[vars]
ENVIRONMENT = "development"
FRONTEND_URL = "http://localhost:3000"

# Real resource IDs and names are added in Phase 5 cloud provisioning.
