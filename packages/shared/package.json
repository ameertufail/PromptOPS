{
  "name": "@promptops/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Shared contracts and utilities for PromptOps Studio",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings=0",
    "test": "vitest run packages/shared/src --config vitest.config.ts --root ../../",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "zod": "3.25.76"
  }
}
